# MiroHedge Phase 0 TODO (freeze v2, 2026-09-18: Bucket-1 repairs, X out of v1, JEV v3)

Legend: `[B]` needs real-browser control (X login, JS pages, portals) · `[W]` normal web search · `[H]` human action · `[D]` doc edit in `plan/`

## 0. Housekeeping
- [x] 0.1 Plan docs frozen-consistent (00-11 + README), pushed to main
- [x] 0.2 DONE 2026-09-18: Phase 0 closed at freeze v2 (Drix10 sign-off, doc 07 log); Phase 1/P1.1 unblocked

## 1. X-Lists research (HISTORY — X out of v1 production per doc 02 §2.6; kept for the record)
- [x] 1.1 DONE 2026-09-18: recovered 55-ID folders table from old MVP config; ~46 Scobleizer AI/tech lists, all live
- [x] 1.2 DONE 2026-09-18: no reusable public macro list IDs exist; TRIGGER coverage = 6-account roster, all verified live
- [x] 1.3 DONE 2026-09-18: 55/55 live, 0 dead, all active; evidence in sign-off log
- [ ] 1.4 DEFERRED to Phase-1 soak: top posters recorded empirically by the sidecar, not by hand
- [x] 1.5 DONE 2026-09-18: self-hosted twikit-rss selected (MIT, list+user endpoints, no X API key); credential placement = Phase-1 item
- [x] 1.6 DONE 2026-09-18, REVISED per user: finance-first universe in 02 §2.4 — 9 finance TRIGGER lists + 6-account roster + 13 market-moving AI/tech CONTEXT; ~40 non-moving IDs dropped from polling (on record in history)
- [x] 1.7 SUPERSEDED 2026-09-18: self-hosted transport has no third-party mirror to fall short; TRIGGER = account roster, throttled only by X frontend rate limits (Phase-1 soak measures)

## 2. Decision-layer proof (doc 03)
- [x] 2.1 DONE 2026-09-18/20: OPENROUTER_API_KEY obtained in env (local, never committed); Phase-0 verification green at `1730acc`.
- [x] 2.2 DONE: Decisions endpoint `POST /api/alpha/decisions` confirmed; revision `typesafe/jev-1.13-20260917`, provider `TypeSafe` pinned into doc 03. (Historical; Phase 2 subsequently accepted/frozen at `50ea369`.)
- [x] 2.3 DONE, RE-RUN 2026-09-18 (freeze v2): 20 v2 cases green under v3 table + 8 new v3 semantic cases (21–28) in doc 03 §3.7

## 3. Venues (doc 01)
- [x] 3.1 DONE 2026-09-18: OANDA v20 practice primary, FXCM demo fallback (REST + streaming, free demo, same shape as live)
- [x] 3.2 DONE 2026-09-18: Alpaca paper primary (free, live-price sim, REST + WS, resettable, no deposit)
- [x] 3.3 DONE 2026-09-18: exchange/broker calendars (fail-closed) + FRED/ALFRED + Fed/ECB pages + EDGAR-derived earnings; all free [HISTORICAL design-lock; operational proof = current Phase-D gate, see the Phase-D entry below]
- [x] 3.4 DONE 2026-09-18: venue names LOCKED in doc 01 ([HUMAN] ACCEPTED)

## 4. Research-plane freeze (doc 08)
- [x] 4.1 DONE 2026-09-18 ([HUMAN] ACCEPTED): langgraph==1.1.6, smolagents==1.26.0, langfuse==4.15.4 (self-hosted); installability verified at build
- [x] 4.2 DONE 2026-09-18 ([HUMAN] ACCEPTED): versions in 08 locked decisions; six-node topology + failure defaults already in 08 §8.3; checkpoint store SQLite(G0/G1)->Postgres(G2+) unchanged
- [x] 4.3 DONE 2026-09-18: exact import allowlist + 3-user OS design (mirotrade/miroresearch/mirohuman) locked in 08 §8.2
- [x] 4.4 DONE 2026-09-18, CORRECTED (reconciliation): features.jsonl schema f2 (§8.5) + R15 table (§8.4) confirmed frozen, no changes needed

## 5. Sources classification (doc 09)
- [x] 5.1 DONE 2026-09-18: Tier tables + finance-first X universe (doc 02 §2.4) leave no blanks; every source has class + failure default
- [ ] 5.2 [HUMAN] Obtain free keys: FRED (signup, 1 key), NASA FIRMS (email token); AISStream/TomTom only if used — say which you want [SUPERSEDED as a gate item: current Phase-D credentials are OpenRouter + FRED/ALFRED + BEA + OANDA practice + Alpaca paper only — see the Phase-D entry below. NASA FIRMS/AISStream/TomTom are Tier-B/C/D, never Phase-D blockers. Kept for history.]
- [x] 5.3 DONE 2026-09-18: EDGAR UA + 10 req/s ceiling locked in 09 §9.2
- [x] 5.4 DONE 2026-09-18: §9.3 heartbeat (>3x cadence = stale) + per-tier failure defaults in §9.1 table; calendar fail-closed
- [x] 5.5 DONE 2026-09-18: ALFRED vintages for replay (locked in 01); Tier D bar + lessons.jsonl already in 09 §9.1/§9.4

## 6. Capital / kill / spend (doc 10)
- [x] 6.1 DONE 2026-09-18: STAGE format + chain frozen in text (pipe-delimited hash, GENESIS, G0 capital 0, alert = alerts.jsonl); [HUMAN] create + sign the G0 file at build per §10.5 steps
- [x] 6.2 DONE 2026-09-18: stage numbers, promotion criteria (necessary-never-sufficient), demotion table, UTC-day rule — all frozen in §10.2
- [x] 6.3 DONE 2026-09-18: SOFT/MEDIUM/HARD (§10.3) + caps/ratio/tiers/anti-flap (§10.4) frozen; §10.5 drills stay build-time boxes

## 7. Calibration + risk constants (docs 11, 05)
- [x] 7.1 DONE 2026-09-18: resolution table (§11.1), base-rate + R13 (§11.1), non-LLM baseline (§11.3), promotion sign-off template (07 log)
- [x] 7.2 DONE 2026-09-18: R1–R9 literals + §5.1a methods + paper fill rule (06 Locked) frozen; R10–R17 as constants; owners = ARCHITECTURE §2/§7

## 8. Freeze sign-off (doc 07)
- [x] 8.1 DONE 2026-09-18 (freeze v2): zero TBDs outside "tune later"; opens = 1.4 deferred, 2.1/2.2 BLOCKED (not needed for Phase 1), 5.2 human-side (not needed for Phase 1 start), 6.1 build-time human step
- [x] 8.2 DONE 2026-09-18: Drix10 signed Phase 0 freeze v2 in doc 07 log — "Bucket 1 complete; JEV v3 semantics frozen; X removed from production v1; Phase 1 unblocked"; Phase 1/P1.1 may begin on human start

## 9+. Build phases
### Phase 1 start checklist (unblocked, non-X sources, no code until human says start)
- [x] P1.1 DONE 2026-09-18: `scripts/freeze-check.sh` 39/39 PASS (human ruling (a): `research_graph_version: g1` declared in doc 08 §8.3; manifest untouched, no version bump, Phase 0 stays closed)
- [x] P1.2 DONE 2026-09-18: core collector complete (`collector/collect.py` stdlib-only, 5 keyless pollers live, session calendar seed); FRED adapter implemented but key-gated (`SKIPPED_CONFIG`, not failure)
- [x] P1.3 FROZEN 2026-09-18: corrective commit (future-check first, canonical effective_at 5-tuple, EDGAR-only amendment contract + unlinked CONTEXT, pinned REF + audit as_of). 19 checks pass; 105-baseline identical (40/42/23). P1.4 may begin.
- [x] P1.4 CLOSED 2026-09-20 (~33h, 133 cycles, 0x403, 7/7 acceptance, 0% pre-grade noise). Evidence frozen (collector/SOAK_MANIFEST.json, 23 files hashed). Report: collector/SOAK_REPORT.md. Shortened 7d->~33h recorded explicitly.
- [x] POST-SOAK LIFECYCLE HARDENING DONE: persisted window.json (restarts never extend), MISSED-cycle semantics (recorded, never backfilled), cwd-independent restart verified, evidence idempotency verified.
- [x] JANE STREET GAP ANALYSIS DONE (no code): js-gap-analysis.md (REMOVED from tree, full text in git history) — 4 satisfied (replayability, deterministic-first, data quality, record conservation), 2 specified-but-unimplemented at the time (battle ladder, incremental DAG — now contracted in plan/13 §13.7 + plan/05 §5.1c), 5 genuinely missing at the time (plausibility tier + self-influenced tagging since implemented in P1.5/doc 11; lineage since implemented; joint-error + regime/decay now contracted in doc 03 case 29 + doc 11 §11.1a). Reconciliation memo: p15-phase2-readiness.md (REMOVED from tree, full text in git history) (HISTORICAL — its "not implemented" statements describe P1.4-close state, not current state; do not treat as a punch list).
- [x] P1.5 DONE: collector/ctx_read.py (f2 bundles, R12/TTL, prose quarantine, heartbeat map, plausibility_v1, lineage) + entity_map.json (entity-v1) + test_ctx.py (16 checks green). Contract fixes pre-coding: canonical_hash in f2 schema + combination rule; conviction nominates/never authorizes 2xR (engine decides); exact plausibility (FROZEN_N=3, pinned map, overnight allowlist); Phase-0 key+endpoint+revision gate.
- [x] P1.5 HARDENING DONE (review-driven): map-sha256 watermarks, entity_ref contradiction, dated-history time-cover, allowlist+hash-format, inference-never-trigger test. 26/26 ctx checks; full suite + freeze green; evidence pristine.
- [x] P1.5 CORRECTION-2 DONE: explicit nominal covers (45m/3h/18h, >=50% span, per-family tests), bundle-vs-feature ownership split + doc round-trip, nested type validation (symbols/timestamps/entity_ref/value shapes, unique hashes). 40+ ctx checks; evidence pristine.
- [x] P1.5 DEFENSIVE-SCHEMA DONE: bool-is-not-int, entity_ref exactly {cik:str}, primitive-first ordering (no TypeError possible), ingested/provenance types, value fail-closed, empty symbols. 55 ctx checks; evidence pristine. P1.5 semantics frozen here.
- [x] P1.5 EXCEPTION-SAFETY DONE: entity_ref/vtype/bundle guards, dated-history shape, JEVAnswerSetV3 contract locked in doc 03 (C++ owns decisions, adapter owns slowness, replay never calls provider). 69 ctx checks; evidence pristine. P1.5 FROZEN. Phase 2 AUTHORIZED (all gates green incl. OpenRouter verification).
- [x] PHASE 2 ACCEPTED/FROZEN at 50ea369 (attempt-counted ceiling, true rolling-30d, invalid-stage fail-closed; 49 checks). Spend interpretation ratified with corrections. Confidence-null omission ratified.
- [x] PHASE 3 STARTED: plan/13-cpp-kernel-build.md.
- [x] P3.2 DONE: committed interop vectors (v1 genuine + v2 stress) + 37-check C++ suite, normal+hardened green; contract frozen in plan/13.2 (signature scope corrected to payload-only); one parser correction (DBL_MAX finite cap) with P3.1 re-verified 102/102. Sidecar untouched.
- [x] P3.2 ACCEPTED/FROZEN (human review of 3fa1573+328d2a1: genuine interop contract, signature-scope correction, DBL_MAX fix recorded as interop correction, C++ test-time independence). NO further P3.2 changes. P3.3/P3.4/P3.5 pending (P3.3 NOT authorized).
- [x] PRE-P3.3 HARDENING PASS 5 DONE (review-driven, transactional/durability slice: `1028d9b`/`4be2563`/`f8382b0`/`3bbb2be`/`1855851`): JEV ambiguous-POST no-retry/no-refund (reservation stands + governor trips, human reconciles) + in-lock cache recheck single-flight (two-process 1-call proof) + unknown-cost HOLD (no answer on unknowable bill) + shared LIVE/REPLAY artifact validator (signature-before-pins, structured HOLDs) + cache_put/log_row failure containment; collector durable-first ordering (signals append is the commit point, validators/schedule after, ConfigError recovery rule); classify ClassifyAbort on DB/infra (no publish, exit 3) + malformed-signals abort + signals-integrity checker verdict + project_day() state-based projection (rerun byte-identical, cycles accumulate, revision_of re-derived, intake stats preserved); soak snapshot-at-creation timestamps with explicit evidence-day + soak singleton (exit 3); ctx history-future bound + ingested>=observed; spend future-ledger corruption flag; docs (03 ambiguity/unknown-cost/single-flight, 09 commit ordering, 10 absolute-means-absolute, 13 budget-dependent gate); +48 freeze pins. Full suite + freeze + C++ normal/hardened + 20k fuzz green; kernel/ + P3.2 vectors byte-identical; evidence pristine. P3.3 STILL NOT AUTHORIZED (awaiting human review).
- [x] P3.1 DONE: kernel boundary validator, 39 checks, sidecar untouched.
- [x] P3.1 CORRECTION DONE: 17 review findings closed (epoch monotonicity, decision-key recompute 0/300 mismatches, universe, confidence quarantine, RFC decode, bounds, dup-keys, timestamps, no-atoll, thread-safe init, repr floats, ed vectors, 96 checks + 20k fuzz normal/hardened). Sidecar byte-identical, P3.2 not started.
- [x] P3.1 X0-FIX DONE: recovered-x==0/sign rejection (y=0/sign=1 valid, y=1/sign=1 invalid, identity round-trip), OpenSSL 3.2 cross-vectors, Ed25519 policy frozen in plan/13, secondaries recorded. 102 checks + 20k fuzz normal/hardened.
- [x] P3.1 ACCEPTED/FROZEN at dcd44d4 (human review: x0 blocker correctly fixed, 102/102 + 20k fuzz normal/hardened, policy frozen, secondaries carried to P3.2/P3.3). NO further P3.1 changes.
- [x] P3.1 AMENDMENT RE-SIGNED (human review of the P3.3 set: constant wrong -> correct LE order, false-reject only, 32-byte change, gates green, no fixture changed; boundary-vector lesson recorded). — `ScalarLessL`'s LE L-table wrong from byte 5 on, false-rejecting ~6% of VALID sidecar signatures (never false-accepting). Root-caused by cross-language bisection to the constant itself (canon/hash/sha512/reduction/point-math all proven equal). Fix = the 32-byte constant only, cross-checked vs Python integers + Reduce512 limbs. Full gates re-run green (P3.1 102/102 + P3.2 37/37 + 20k fuzz, normal+hardened). No fixture changed.
- [x] P3.3 ACCEPTED (human review at `41f3b66` over correction `7d83974`): full §3.4 state, closed schemas, checked ints, micros freshness, symbol binding, hardened kernel, veto codes; 78/78 + SHA-256 replay + §3.7 21–28. Locked: v2 1–20 HISTORICAL, feature_revision frozen. P3.4 = (b).
- [x] SLICE A DONE (authorized; compiler-enforced request authority): ValidationRequest private-immutable (const fields, deleted assign, private ctors) with exactly 2 friends (KernelState + validate_jev); request_for() sole construction path; kernel/auth/ negative compile harness (6 probes fail for intended reasons) + positive control (compiles, runs, copy validates identically) + occurrence-count friend gates in build.sh; gate-hardening fix `f8205c2` (Slice A fully closed per review); P3.1 102/102 + P3.2 37/37 + P3.3 78/78 + 20k fuzz green normal+hardened with vectors/reasons/counts unchanged (harnesses route through KernelState; no fixture touched).
- [x] SLICE B DONE (authorized; pure risk veto): `kernel/risk/veto.hpp` + `kernel/risk/veto.cpp` (RiskSnapshot -> HOLD/PROCEED + frozen reason, K6 formulas, stage multipliers, R13 floor, R7 drift selection, BuildEngineInputs onto frozen P3.3); 109-check suite incl. 2 composed veto+table rows on committed artifacts; JEV-isolation grep gate (no answer reads in veto); P3.1 102/102 + P3.2 37/37 + P3.3 78/78 + 20k fuzz green normal+hardened, P3.1-P3.3 files untouched. CORRECTION `7bc11a9` (review blockers: stage-aware leverage G1-1x/G2-2x+1x, fixed-storage allocation-free verdict + drift index + static_assert, invalid enums fail closed with exact-EXIT bypass, overflow-free R3; 128/128). CORRECTION 2 `26f5c7b` (review round 2: structural validation before EXIT bypass, realized_outcomes/flip-record/Stage-enum/clock validation, MEDIUM unconditional-HOLD frozen in doc 03 §3.4; 144/144). CORRECTION 3 `ad2a35b` (review round 3: EXIT-intent vs ENTRY-riskstate validation split — valid exits bypass corrupt risk state, corrupt intent still holds; margin_used/symbol/drift-candidate integrity; H1 drift-ordering contract pinned; 153/153). Slice C correction pass DONE (5 audit findings fixed at `ddf1a6c`, awaiting human re-audit; Slice C stays OPEN until confirmed): (1) `FindAscii` replaces every `find(U8(...))` — no key temporaries on the ingest path; (2) OVER_COUNT counts `dropped` only, disjoint from `rejected`; (3) value checks reordered to exact P1.5 precedence (unknown-type beats key-count) + multi-defect regressions, P1.5 cross-run confirms `schema-value` on both; (4) `ShouldAlert` widens to `__int128` before any addition; (5) `RateAdd` uses `__int128` elapsed, rollback keeps the window, counters saturate. Runtime proof `kernel/ingest/test_noalloc.cpp` (wrapped-malloc counter: 0 allocs over 65 retains + 100 rejects) runs in build.sh normal+hardened alongside the grep gate (now also forbids `U8(`/`.find(`). Suite 78/78, P3.1-P3.3 + veto + fuzz + freeze + evidence green. Slice C CLOSED/ACCEPTED per human re-audit (`ddf1a6c` code). Follow-up cleanup at `04d9306`: tumbling-window wording, LF endings restored on `build.sh`/`freeze-check.sh` (future diffs minimal), `.gitattributes` pins `*.sh` to LF. Slice D NOT AUTHORIZED.
- [x] PHASE 2 DONE: collector/jev.py (v3 batch, pinned revision/provider, RFC8032-Ed25519 sign/verify proven vs independent oracle incl. real clamp-bit253 bug found+fixed, research/decision cache, retry-once-HOLD, spend ceiling, zero-network replay). 40+ JEV checks; authority boundary tested (no size/budget fields). Awaiting human diff review.
- [x] NEXT (historical): Phase-0 OpenRouter verification is DONE (`1730acc`); Phase 2 jev.py was authorized on that basis and is now ACCEPTED/FROZEN (`50ea369`). No open verification item remains.
- [x] POST-SOAK LIFECYCLE HARDENING SUPERSEDED by pre-P3.3 pass: crash-consistent classifier publication (commit-then-atomic-rename), atomic soak/audit/acceptance artifacts, window-config validation, fsync'd polls. Secrets contract DONE: .env.example (docs in header) + root .env (untracked) + collector/config.py; history/tree audited clean; no OPENROUTER or other unused credentials introduced.
- [ ] PRE-D GATE (authorized; Slice D stays NOT AUTHORIZED until accepted): [x] Phase A research reconciliation (memos verified against source, no re-research); [x] Phase B contracts (doc 03 case 29 + test row, P3.3 81/81; doc 11 §11.1a regime/decay with H=infinity default; doc 05 §5.1c risk DAG; doc 13 §13.7 battle ladder); [x] Phase C TODO reconciliation; [x] PRE-D RE-AUDIT CORRECTION (`228d923` tests + `6d520f1` docs): case 29 strengthened from forced-veto to real R2 pending-risk end-to-end (veto suite c29-*: Clean + $66k pending on other symbol = 76% > 75% cap, EvaluateVeto -> pending-risk -> BuildEngineInputs -> table HOLD engine-veto:pending-risk; P3.3 row kept as table-isolation half with hardened harness); wording corrected to answer-proxy (fixture pending_exposure_pct is 0.0); regime daily representative (last valid 1h obs/symbol/day, UNKNOWN breaks adjacency) + weight composition (inclusion x decay, same weights both sides, ESS + floor reported) + harness config identity; DAG L1 measurements vs L2 predicates; ladder single ownership (feed-F vs JEV-H1 split, version-skew to Phase 2.5, causal order wording); `.gitattributes` cr-at-eol so `git diff --check` is clean. Gates: P3.3 83/83 + veto 160/160 normal/hardened, P3.1 102/102, P3.2 37/37, ingest 78/78 + NOALLOC, 20k fuzz, freeze PASS, evidence pristine; [x] PRE-D FINAL HARDENING (`25e3441` + `f629784`, verified before recording): c29-only-pending-veto isolation assertion (veto 161/161 both modes; n_reasons==1, reasons_all[0]==pending-risk) + UNKNOWN-regime wording (decay_weight=1, inclusion preserved, adjacency still broken). Full battery re-green (P3.3 83/83, P3.1 102/102, P3.2 37/37, ingest 78/78 + NOALLOC 0, 20k fuzz, freeze, CTX/JEV, evidence pristine, diff --check clean, no ending churn, no production behavior change). PRE-D GATE A–C: CLEAN (human-confirmed); [x] Phase D: Phase 2.5 research plane (`e21546f` writer+resolver+round-trips, `ab0db56` graph+R15+cadence+attribution+workers, `ed607ef` lessons+R12+sandbox+isolation, `215a958` Tier-A+calendar; `PHASE_E_AUDIT.md`): 6-node LangGraph + SQLite checkpoints (crash-resume, no-dupe proven), R15 caps (loop-stub abort proven), atomic emit (temp+fsync+rename+manifest, idempotent, partials invisible), deterministic resolver (source only on mechanical identity, else inference; confidence computed), cadence gating (TRIGGER-immediate, 30-min TTL, throttle doubling, estimate check), spend attribution log (per-node/model/day + 30d projection), import-scan gate, write-confinement proof, live EDGAR/Fed evidence (all-200, zero-403, p50 ~78ms), calendar fail-closed, 12 graded lessons. DEPLOYMENT BOXES (explicit, Linux build host): OS users + setpriv run, egress-proxy probe, image digest/SBOM/scan, Langfuse server, FRED/ALFRED keys, Tier B/C harvest, 7-day run. [x] Phase E dependency audit: no D–H1 interface changed (G gains H=infinity default + harness identity fields only); kernel/collector/plan untouched by Phase D. - [x] PHASE-D THIRD AUDIT PASS (`aaa8ecd`, 26 end-to-end findings, verified before recording): hypothesize/critique are pure build/parse callables and the GRAPH performs every provider call through required model_factory GatedModels (arbitrary deps cannot reach the provider; two-thread test proves disjoint cycle/budget/span identities); cycle_id stamped from thread_id per run (budget_factory per attempt; run_cycle(budgets=) removed); budget ledger is SQLite with per-(cycle,symbol) counters + lease rows (corrupt/oversize/missing-existing NEVER resets — AbortCycle; double-settle/unknown-lease abort; 7-day retention prunes without touching live cycles); token ceiling is hard (max_tokens clamped downward only, prompt+completion reconciled, breach/unaccountable/negative usage aborts post-settle); timeouts invalidate the budget row (leaked-thread late writes cannot corrupt; repeated-timeout thread-count proof) + per-run executor cleanup in finally (proof) + run_in_process true-kill primitive; provider egress forced through deployment proxy via explicit HTTPTransport (missing proxy blocks; note: httpx.Client(proxy=...) is silently ignored by some versions — transport pinning is the enforcement); frozen spend governor G0/G1 $150 G2 $400 G3 $1000 with 60/80/100% tiers driving throttle/triggers-only/deny (deny aborts with accounting; missing pricing blocks; unmeasurable ledger denies); attribution category=doc-10 taxonomy + separate outcome field, mirror appended only on insert, missing ledger raises (never $0), atomic mirror regen; digest write precedes freshness (IO failure keeps stale, proof); thesis >500 rejected never truncated; producer enforces 16-symbol cap; watermark/history validated producer-side (coverage gaps drop, malformed fails closed); manifest seen-marks only verified rows + field validation; checkpoint saver constructed with explicit strict serde (fail-closed, effective-config proof; with_allowlist documented as insufficient); single-run-per-process guard (concurrent run_cycle refused, proof); watchlist/trigger/stamp/history/fused-candidate/prompt bounds; hard iteration ceiling with overrun flag (infinite-producer proof); raw-record validation before orchestration; graph-stamped origin (candidate self-promotion proof); single process-wide lock guard (no per-path registry). Batteries: plane 77 + 16 + isolation + sources green (venv), stdlib subset green on system python; kernel re-green (83/83, 161/161, 78/78, NOALLOC normal+hardened, fuzz, freeze, CTX/JEV, evidence pristine); kernel/collector zero-diff. Open deployment boxes: Docker daemon + egress-proxy probe, image SBOM/scan, model key, supervisor WALL_S kill, Langfuse tail; no GitHub CI (no workflows). Slice D NOT AUTHORIZED.
- [x] PHASE-D FOURTH AUDIT PASS / THIRD-PASS CORRECTION (23 findings: 10 P0 + 9 P1 + 4 P2 against `aaa8ecd`, closed agent-side in `790374d`/`851725a`/`c3ea793`/`ab92bf0`, verified before recording, NOT human-signed): P0 — pre-call dollar HOLDs at the run_gated boundary (worst-leg pricing, single governor-owned table; $149+$2 refused pre-spawn with factory provably untouched; multi-symbol holds compose); frozen T0-T3 tiers implemented exactly (T1 double-interval + critique-TRIGGER-only + NULL-suspend; T2 cheapest-model + 200-char cap + watchlist-2 + SOFT_KILL sentinel+hook; T3 deny + MEDIUM_KILL) on the trailing-7d-run-rate x30 projection with hourly journaled evaluation, immediate escalation, 6-consecutive-hour anti-flap fallback, and G2/G3 ratio (suspended without a profit feed, 3-failed-days forces T3); ambiguous attempts (timeout/child-crash/provider-error/unaccountable-usage) settle FULL reservations as UNKNOWN_SPEND (never $0), keep holds against the cap, poison R15, and deny future spend until supervisor reconcile_unknown (proven end-to-end incl. unblock); graph owns construction (no parent-side model objects; refused reserve = factory never runs, proven by touch-file; raw factory = clean config-error with zero spans); TRUE pre-call token bounds (prompt utf-8 bytes under stated byte-BPE assumption + downward-only 1500/step clamp + agentic closed-form bound; over-cycle bounds refuse pre-spawn; breach tripwires abort); ledger deletion fails closed via init markers (fresh vs deleted distinguished; supervisor fresh-start = delete both); integrity_check RESULT read + exact schema pin + user_version (self-calibrating corruption proof); any post-call accounting failure aborts hard (mocked-span proof, no downgrade); production boundary is spawn-kill with SIGTERM-to-SIGKILL escalation + verified reaping (active_children empty) + post-mortem container reclaim (never while live). P1 — strict map schema/bounds (bad CIK/ticker/keys/macro/note rejected); latest_complete/read_latest verify envelope semantics (sha-valid-but-void generation skipped); full 256-bit bundle/feature IDs; pre-materialization bounds (len-gate + islice; 100k-dict probe); strict json_safe typed validation (no default=str admission); no thesis/critique coercion (non-string/non-dict rejected); thread_id/epoch control-plane validation; attribution row hardening (finite non-negative USD, bounded ints/strings, enum CHECKs, NaN-rejecting DDL, user_version). P2 — WAL/SHM-aware bounded storage with checkpoint/vacuum reclaim + 120d span prune; digest O(1) seen-cache; per-purpose in-process locks (no global serialization, no per-path leak); CI workflow (stdlib + plane + kernel on push; first hosted run pending). Batteries: plane 106 + emit 19 + isolation + sources green (venv), stdlib subset green on system python; kernel re-green normal+hardened (83/83, 161/161, 78/78, NOALLOC), freeze, CTX/JEV, evidence pristine, diff --check clean; kernel/collector zero-diff. Prior third-pass spend/boundary closure claims are SUPERSEDED by this pass (the re-audit was right). Open deployment boxes: Docker daemon + egress-proxy probe, image SBOM/scan, model key, supervisor WALL_S kill, Langfuse tail, profit feed, calibration feed. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D FINAL HARDENING PASS (32 findings against `151000a`, closed agent-side in `45996e9`/`1c4d0d2`/`a2780ae`/`cc1beac`/`1af480a`, verified before recording, NOT human-signed): P0 — single-transaction dollar authorization (reap+block-check+measure+compare+insert; 4x$0.60 vs $1.00 cap admits exactly one across processes; loser never touches provider through run_gated); model/pricing identity equality pre-reserve (factory untouched, no hold, no span); corrected 4-slot tool-growth bound + per-call tape fit-proof with message-object projection (enum roles, list content) and clean config-error refusals; tri-state markers both ledgers (invalid never folds to absent); atomic unknown record (span+row+invoked, v2 schema with hold span_id) + reconcile-from-everywhere (hold-only/span-only/complete/loud-empty) + invoked-hold backstop; zero-price timeout with no dollar block and proven next-cycle success; all post-call accounting fail-closed with snapshots (mocked record_unknown proves unresolved-spend abort + hold retained + deny); deterministic IPC (bounded get, no empty()); child validates everything pre-factory (touch-file proof); bounded tool outputs (100MB repr never invoked); child result caps (256/1MB/16KB); per-producer drains (infinite parser+worker terminate); harvest envelope validation (11 malformed shapes blocked-or-counted, nothing published); fail-closed tier state (deleted/corrupt/future/binding) + single-lock journal/state with tail recovery (injected crash proven); distinct-day ratio (5-same-day safe, 3-days forces, ok/suspended break); mandatory stateful governor with interface check; sole governor pricing (divergent deps table proven ignored); strict span identity (idempotent-vs-conflict + race recheck); lease ownership + underflow + cycle-bound + loud invalidate; strict publisher inputs + map rejections; manifest strict rows + bounded tail + rotation + reader-fallback; digest conflicts + bounded memory + evicted-key file scan; sentinel-required signals (hook visible, sentinel failure aborts publication); transactional tier journal/state; lifecycle stress (8 timeouts reaped each, reap-after-death observed, no success-path reap); strict timeouts/containers/model-ids; type-exact finite numerics (bool/inf pricing/amounts/calibration/projection rejected); dead code removed (stub_advisory, make_model, combine_hashes/SEP, run_with_timeout + test); CycleBudget retained as tested oracle (equivalence proven); sync_mirror wired to hourly prune (self-heal tested); emit_bundle hostile-input guard; reprlib-bounded errors/IPC/snapshots. Batteries: plane 105 + hardening 55 + emit 19 + isolation + sources (venv OK), system-python stdlib subset OK (24 skipped dep-gated), kernel normal+hardened 83/83+161/161+78/78+NOALLOC, freeze-check PASS, CTX/JEV PASS, evidence pristine, diff --check clean, static audit (no except-pass, no empty(), no OR-IGNORE spans, bounded lists/reprs/fetchalls dispositioned), kernel/collector zero-diff. Hosted CI on `d79253b`: plane SUCCESS (105+55+19, independent of local output), kernel SUCCESS, stdlib FAILURE at the collector step (pre-existing at `151000a`, unreproducible locally on Win/Linux 3.11-3.13, frozen code outside the touch zone — needs admin log access; reported, not fixed). Concurrent ledger first-creation is serialized on a dedicated create lock (WSL-proven 8/8 + hosted plane green). Open deployment boxes unchanged (Docker/egress/SBOM/key/WALL_S/Langfuse/profit-feed/calibration). Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D DEPLOYMENT/EVIDENCE PASS (authorized; from `36c2862`; Slice D untouched, kernel byte-identical, collector production code frozen with one disclosed additive exception (`RESEARCH_MODEL_ID` loader key, Addendum 32), no live capital, fail-closed intact; audit-accepted with accounting corrections): Boxes 1/2/3/5 PROVEN (isolation tree + 4/4 + 8/8 probes; squid exact allowlist + internal net 5/5, tinyproxy rejected as CONNECT-blind; pin+digest+CycloneDX194+scout baseline; WALL_S ladder Linux 4/4). Box 4 PROVEN live (`e00d4be`: OpenRouter 15+16 tokens $0.000005 via Squid, reserve→span→settle; label = LIVE PROVIDER + ACCOUNTING DEPLOYMENT PROOF, not a full graph cycle). Box 6 PARTIAL (server healthy 200 + ledger attribution; full-day live BLOCKED on key). Box 7 PARTIAL with exact Tier-A matrix: EDGAR/calendar/Fed-ECB-RSS/Treasury/BLS PROVEN live; FRED-ALFRED PROVEN 2026-09-23 (`7ce8a47`: GDP observations + deterministic realtime replay + bad-key fail-closed, one credential); BEA BLOCKED (key); broker feed OPEN (no creds, unattempted); earnings calendar implementation+probe PROVEN 2026-09-22 (`ca22ebb`: EDGAR-derived veto gate, live AAPL/MSFT 200s, True-on-event/False-on-quiet/True-on-unknown) but Tier-A operational wiring (poller+TTL+heartbeat) OPEN per §9.4 — source NOT YET FULLY PROVEN; lessons 12/12; profit/calibration feeds are FUTURE-STAGE (not Phase-D blockers); registry push OPTIONAL (frozen req = pin+digest+SBOM+scan, all recorded). Box 8 PARTIAL (40-epoch kill-9 both branches proven + no-dupes; 7-day + wall-clock rate BLOCKED on time). Deep-review hardening pass (`1349930`: fixture-clobber guard, idempotent proxy bring-up, C:-workdir split-brain fix, workdir/stray hygiene, honest clocks, dead-code removal; `mirohuman` nologin ruled NOT A BLOCKER — script unchanged). Local 255 green + freeze PASS + kernel exit 0; hosted 35769648294 plane/kernel/evidence SUCCESS, frozen stdlib FAILURE. `36c2862..cea59b8` = 9 commits (7 box + hardening + accounting; docs record inside). Slice D NOT AUTHORIZED. Phase D OPEN — blocked only by externals/elapsed-time now.
- [x] 2026-09-23: BEA PROVEN (datasetname param fix; list+50-row T10101+replay+bad-key; Addendum 42). OANDA practice BLOCKED (first-party India denial, Addendum 39 � human decision pending). Alpaca paper PROVEN (Addendum 40). Re-audit hardening (Addenda 45-47): heartbeat strict schema + SEC validation (39/39), kind-tagged probe denials, semantic replay, readiness wording; hosted 35898494106 all-green.
- [x] REPO HYGIENE PASS (auditor-directed, `01b6e47`+`6fca6eb`, no credential work): (1) env consolidation — canonical root `.env`+`.env.example`, sole loader `collector/config.py`, retired tracked `sandbox/provider.env.example` + its gitignore rule, `.env.example` gains consumed `OPENROUTER_API_KEY`; (2) dead-material audit — `/research` RETAINED (TODO-referenced HISTORICAL audit trail, not scaffolding), nothing else dead in full-tree audit; (3) tests organized, never deleted — `collector/tests/` (6 suites, HERE→parent one-liners) + `kernel/tests/` (4 files, `../` includes), `build.sh`+CI+README paths follow, freeze gate updated for new p33 path (intent preserved, risk/ingest precedent); freeze PASS + kernel build PASS from new layout. Rule going forward: tests live beside components, updated with implementation, everything re-run.
- [x] PHASE-D STATUS CONFIRMED
- [x] PHASE-D STATUS CONFIRMED against `06da85c` (human verification, no code change): Round 9 CLOSED, narrowed threat model ACCEPTED, independent CI evidence CLOSED. Phase D NOT YET CLOSED — deployment/operational exit boxes explicitly open (Linux isolation deployment, egress enforcement probe, image digest/SBOM/scan, model credentials/config, supervisor WALL_S kill path, Langfuse attribution, required source/config feeds, unattended operational evidence; plan/08 §8.6 + plan/09 §9.4). Security-model question finished; deployment-readiness question not. PRE-D sequencing holds: Slice D NOT AUTHORIZED until the Phase-D gate is accepted. `06da85c` remains the clean documentation checkpoint.
- [x] PHASE-D ROUND-9 SIGNED OFF (human decision, narrowed model ACCEPTED): coherent rows+digest forgery is REAL but unfixable in-substrate without an independent trust anchor — no code change made, no theater introduced. Boundary recorded: rows↔digest = mutual-consistency/corruption detection; marker = recovery mirror + shape tripwire, NOT a trust anchor; host/filesystem integrity = outside the trust model. HSM/TPM/remote witness is a NEW security architecture, not a follow-up patch (unfunded). Independent CI evidence CLOSED (evidence job green on hosted). Round 9 closed. Phase D eligible to close pending remaining sign-off criteria/deployment boxes (NOT declared closed here). Slice D NOT AUTHORIZED.
- [x] PHASE-D ROUND-9 RESPONSE (new P1 against `bb4733a`: coherent rows+digest forgery needs no marker touch — CONFIRMED via throwaway PoC, not committed): no ledger behavior change — prevention is impossible in-substrate without a secret/external anchor (legit crash-lag auto-recovery, pinned by 116 tests, is observationally identical to coherent forgery given only DB+marker; key files/triggers on the same disk are theater against a disk-write actor; that actor is host-compromise class). Threat model narrowed explicitly per the auditor's accepted alternative: ledger proves mutual consistency + single-object deletion + crash atomicity + gated migration vs non-coherent faults; coherent multi-object forgery bottoms out at host/filesystem integrity. Residual corrected rows+digest+marker → rows+digest (marker fields are a lag-tolerant recovery mirror + shape tripwire, NOT an authority root — code comments + plan 08 say so exactly now). CI evidence gap closed: new independent evidence job (isolation+sources) decoupled from the frozen collector loop. Batteries re-run green locally (see commit); kernel/collector zero-diff. Hosted run 35747745650 on `4dcf137`: plane SUCCESS, kernel SUCCESS, evidence (new isolation+sources job) SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed — awaiting human sign-off on the narrowed model.
- [x] PHASE-D ROUND-8 FIX PASS (2 P1s against `ce70100`; tier/timestamp/ratio/init closures verified by the human and untouched): digest-deletion reset closed on both ledgers — digest table mandatory at schema v3, NEVER rebuilt (missing digest on a digest-era ledger denies digest-deleted); one-time migration only for provably pre-digest ledgers (pre-digest version AND pre-digest marker shape; version reset alone cannot reach it); round-7 outputs upgrade without recompute; crash resumes mirror from in-DB truth; cycles-drop+reset variant closed by the same gate; seen-deleted removed as unreachable. Witness-first sidecar publication (flag before file; init-crash resume proven). Residual: full marker forgery + version reset (documented coherent-forgery class). Auditor nit accepted: rev=0 revisioned row can pass chain predicates but only toward more-restrictive states (P2, non-blocking, unchanged). Batteries: plane 108 + hardening 116 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted rerun pending on push. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D ROUND-7 FIX PASS (6 findings against `48d5bd2` — six, not seven; the CRLF was a fix detail — closed agent-side in `ce70100`, verified before recording, NOT human-signed): same-transaction in-DB content digests on both ledgers (XOR canonical hashes + exact counts/cents, explicit per-mutation maintenance; seam closed by SQLite atomicity); marker mirrors with adopt-on-consistent (self-healing lag, tamper erased, strict slot validation, no defaults/NaN); sidecar one-time-migration flag (seen-deleted only when digest reconstructed that open, else re-adopt); tier legacy boundary (rev-less recovers only at rev 0 with no revisioned rows; newer denies, older audit-only); future tier/checkpoint timestamps + naive-checkpoint rejection; ratio tail scope documented (no code change). Interop: out-of-band-edit tests retired to digest-mismatch (old codes remain as backfill-evasion guards); prune-aging via now_wall; conflict test plants digest-coherently. Batteries: plane 108 + hardening 113 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted run 35716617708 on `1a8d48d` (round-7 code; docs-only delta): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Hosted run 35741330249 on `55e1eb0` (round-8 code; docs-only delta): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D ROUND-6 FIX PASS (7 findings against `fb236d5`, closed agent-side in `48d5bd2`, verified before recording, NOT human-signed): historical row integrity (marker-carried cumulative roots on attribution — max-ts + pre-declared prune waters, append-only unknown count, hold created/closed — re-baselined post-commit under lock; out-of-band 30d cycle-registry sidecar on R15; joint/registry-only/row deletion and same-schema DROP+CREATE all deny; present-but-empty backfill heal removed as cover-resurrecting; marker-absent over live rows denies; pre-roots markers TOFU-adopt once); future start_wall denies on read+write (CLOCK_SKEW_S 300s); tier journal is a semantic chain (consecutive revs from Tier 0, snapshot match, max rev == state rev, newer-than-state is forgery, non-legacy never adopted); ratio journal strictly increasing days; single-transaction first init + pristine re-init on both ledgers (no DDL outside); checkpoint-id presence decides timestamp-less snapshots. Interop found while verifying: the residue-heal masked registry-only deletion (removed); a Python rewrite script CRLF'd the test file (fixed to LF, amended). Batteries: plane 108 + hardening 105 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted run 35710113948 on `86cd363` (round-6 code; docs-only delta): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D ROUND-5 FIX PASS (8 findings + radar note against `0dccc0b`/`3e3a18b`, closed agent-side in `fb236d5`, verified before recording, NOT human-signed): established DBs never regrow tables (table-missing deny; may_create only for first-init/pristine; unreadable catalog denies); cycles migration truly transactional + version-explicit (v2; v1+table adopts, v1-table migrates, v2-table-missing denies); stale-thread lookup failure rejects (checkpointer-less apps only proceed blind); extract reservation covers measured agent framing (32 KiB overhead, proven vs exact messages; tape backstop); budget numerics fail closed (DDL CHECKs + read validation; corruption aborts, never normalizes); ratio/tier journals strict (exact schemas, tier_rev tripwire, state-first persist, legacy recovery kept); cleanup before shape rejection (extracted shaper unit-tested); timeout documented as prompt-termination + bounded escalation; retention list failure raises; trusted-config boundary recorded in plan 08-8.4. Interop found while verifying: file-layout change moved a corruption-probe offset (test now catches decode errors) and retired one outdated migration test to the version gate. Batteries: plane 108 + hardening 96 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted run 35682744131 on `0229bd7` (round-5 code): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D ROUND-4 FIX PASS (10 findings against `c423e20`, closed agent-side in `0dccc0b`, verified before recording, NOT human-signed): Tier-2 requires the durable signal_dir sentinel (hook alone no longer gates); migration CREATE+backfill in one transaction + idempotent crash-residue heal; unreadable marker/state/journal fail closed (only provably-missing is fresh; ratio tripwire + strict reader with truncation detection; suspended days never journal); run_cycle refuses checkpoints older than the 7d R15 window; generate text byte-capped pre-IPC (parent aligned); extract cleanup rides the envelope with authoritative parent reclaim; emit records r15-budget-unreadable; kill ladder begins at the deadline (0.25s settle grace; 2s deadline resolves ~2.3s). Forensic note (accepted, no change): synthetic recovery spans keep epoch=0/cycle=recovered/symbol=? (unknown_holds carries no cycle/symbol link; dollars conservative). Batteries: plane 108 + hardening 82 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted run 35643930706 on `30be8a8`: kernel SUCCESS, stdlib pre-existing failure, plane FAILURE on test_extract_cleanup_failure_reaps_and_blocks with EMPTY blocked (log via signed-in browser; API logs admin-blocked) — diagnosed as environment-dependent reclaim test + silent extract-abort evidence; closed in follow-up `3e3a18b` (hermetic reclaim, extract-aborted evidence, _reap_container contract test, hermetic partial-frame test replacing a Windows-vacuous subprocess version). Hosted run 35646920298 on `4370574` (follow-up code): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D ROUND-3 FIX PASS (3 mandatory + 1 pathological findings against `6fe44d5`, closed agent-side in `c423e20`, verified before recording, NOT human-signed): R15 read paths fail closed (`snapshot`/`check`/`settle`/`invalidate` abort on a deleted recent cycle — the cycle estimate can no longer read zeros); cycles registry backfilled from counters during old-ledger migration (same init, fail-closed); tier `evaluate` holds ONE lock across load-compute-persist (locked persist/ratio variants, tier-first order; two-governor stale-writer regression + single-acquisition proof); Pipe frame read bounded by the call deadline (stalled mid-frame senders hit the kill path; reader joined post-kill; dead-child/exit race settled so labels stay exact) + partial-frame and 1MB-streaming regressions. Batteries: plane 105 + hardening 73 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted CI on `82247b3` (run 35636651055, same code tree): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D ROUND-2 FIX PASS (5 P0/P1 + 3 small findings against `5f54ee5`/`a7bbbde`, closed agent-side in `6fe44d5`, verified before recording, NOT human-signed): Pipe reads WHILE the child runs (large results stream; envelope proves completion; missing envelope loud; unkillable child never success) + 1MB regression; hold-only reconcile mints the synthetic span first (span evidence asserted) + actual > reservation hard-rejects pre-mutation; success-path settle failure aborts with the hold retained; R15 cycles registry (missing row for a seen cycle aborts; prune-aged recreates); ratio day-record under one lock; duplicate timeout test removed; map bound truly 1MiB+1; pre-provider test asserts the retained reservation; timeout test follows the reservation contract (not-billed attests $0). Batteries: plane 105 + hardening 69 + emit + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted CI on `349e89a` (run 35631946891): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step. Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D RE-AUDIT FIX PASS (5 human findings + IPC against `62e1019`, closed agent-side in `5f54ee5`, verified before recording, NOT human-signed): strict `record_unknown` (hold-usd equality, reserved/invoked state, unknown_holds identical-idempotent vs conflict-loud, rowcount-verified transitions) on one shared span-identity helper with the normal path (ts metadata-excluded); fixed a REAL shipped stage/symbol column swap the strictness exposed; tier journal+state under ONE lock acquisition (locked variants); pre-provider unwind failures abort with both-sides snapshot (reservation preserved) + verified `mark_invoked`; `run_in_process` Pipe handoff (10s anomaly bound, Windows BrokenPipe loud). 9 new regressions; crash-seam test follows the locked layer. Batteries: plane 105 + hardening 64 + emit 19 + isolation + sources + stdlib green locally, kernel build.sh exit 0, freeze PASS, kernel/collector zero-diff. Hosted CI on `c5634f8` (run 35626652683): plane SUCCESS, kernel SUCCESS, stdlib FAILURE at the same frozen-collector step (pre-existing, untouched per authorization). Slice D NOT AUTHORIZED. Phase D NOT closed.
- [x] PHASE-D SECOND AUDIT PASS (`a921934`, 32 end-to-end findings, verified before recording): exactly-once manifest under inter-process lock + unique temps (concurrent same-bundle multiprocess proof); latest_complete resolves newest VERIFIED generation by (epoch, sequence) with corrupt-newest fallback; read_latest consumes a private snapshot of verified bytes (swap-between-verify-and-read proof); R15/token/attribution enforced at the real invocation boundary (GatedModel/PrescanningExecutor/gate_tool with reserve-before-execute, token reservation 6250/call reconciled to actual usage, one span per attempt incl. error/timeout, fake multi-step proof: 3 LLM + 2 exec = 5 spans); hung calls abort promptly with the attempt charged (thread watchdog) + true-kill run_in_process primitive for the supervisor contract; crash-durable (cycle,symbol) budget ledger (resume reconstructs, no fresh mint); abort short-circuits remaining work (extract 1 call not 5); cadence advances only on success + merge-under-lock persistence + bounded load; per-cycle estimate fed from budget totals; attribution = SQLite UNIQUE(span_id) ledger with doc-10 schema (stage/cycle/symbol/prompt/completion/usd/category) + mirror repair + concurrent dedupe proof; 30-day retention via delete_thread + strict msgpack allowlist; bounded iteration everywhere + full checkpoint-field caps (watchlist/triggers/stamps/blocked/history/thesis/candidate bytes); publish binds ONE map file (sha over bytes, resolver gets its parse), deterministic 64-cap with over-cap count (101->64 proof), lineage-derived feature IDs with dedupe; parser_confidence in the downgrade chain (low parse on high tier -> low); canonical-shape validation + unknown-CIK reject in resolver; sandbox spec fully locked (authorized-imports vs package-installs separated, build_new_image=False, digest-pinned, proxy network REQUIRED else ConfigBlocked); Dockerfile base digest + package pins; research_digest.jsonl writer (idempotent, over-long rejected); trigger_symbols explicit cycle input (proof); history first-class harvest->bundle->reader (proof); health_hook supervisor contract (proof). Batteries: plane 50 + 16 + isolation + sources green; kernel re-green (83/83, 161/161, 78/78, NOALLOC, fuzz, freeze, CTX/JEV); kernel/collector zero-diff. Open deployment boxes (need Linux host, unchanged): Docker daemon + egress-proxy probe, image SBOM/scan, model key + pricing table, supervisor process kill at WALL_S, Langfuse tail. Slice D NOT AUTHORIZED.
- [x] PHASE-D AUDIT CORRECTION (`6965b89` + `49ecb65`, human re-audit findings, verified before recording): bundle identity covers epoch+features+watermarks+history (changed-cursor re-emit gets a new id; manifest stays consistent); POSIX fsync fail-closed (Windows skip documented); manifest symlink/escape hardening + `read_latest()` as the load-bearing manifest->reader path; resolver canonical kind in the source-identity check; real CodeAgent/DockerExecutor worker path (blocked paths tested; live Docker execution = deployment box); ConfigBlocked mapped to per-node failure defaults; R15 charged at hypothesize/critique boundaries + fail-closed on unwired budgets; cycle_aborted returned as state (abort -> emit publishes nothing, proven); cadence persisted across restarts (proven); PlaneHealth trailing-1h recomputed window with recovery (proven); attribution span_id dedupe (proven); real production publish step (resolver+emit, empty publishes nothing); checkpoint caps RAW512/CAND256/FUSED128/SYM64 (proven); AST import scan incl. dynamic loads (documented Docker-boundary scope); requirements fully == pinned; FRED READY only after live probe; calendar presence-gate scope stated. Plane battery: 13 emit + 24 graph/R15/cadence/attribution + 3 isolation + 3 calendar green; kernel battery re-green (83/83, 161/161, 102/102, 37/37, 78/78, NOALLOC, fuzz, freeze, CTX/JEV, evidence, diff-check). Slice D NOT AUTHORIZED.
- [x] P1.4 acceptance automation DONE 2026-09-18: `soak_check.py` (6/6 objective checks: edgar-403, heartbeat vocab, replay determinism, future leakage, SQLite integrity, boundary) + `pregrade.py` (day-1: 40/40 auto_genuine, 0 ambiguous → zero human reviews needed today). Human work reduced to `data/soak/human-queue.jsonl` only. Both wired into soak daily cycle. No rules_v1 change.
- [x] P1.4 CLOSED (shortened): ~33h / 133 cycles, 0x403, 7/7 acceptance, 0% pre-grade noise (see collector/SOAK_REPORT.md + SOAK_MANIFEST.json). The original 7-day target was closed on this evidence; NOT a 7-day run.
- [x] P1.5 DONE (stub superseded): `collector/ctx_read.py` (f2, R12/TTL, prose quarantine, entity-v1, FROZEN_N=3, canonical_hash); 69 ctx checks; P1.5 FROZEN.
- [ ] Later: 11. Phase 2.5 research plane (PRE-D GATE Phase D, authorized next) → 12. Phase 3 C++ core cont. (P3.1 RE-SIGNED; P3.2 FROZEN; P3.3 ACCEPTED + case-29 row, 81/81; P3.4=(b); P3.5 slices A–C DONE/CLOSED, D NOT AUTHORIZED, E ACCEPTED/CLOSED (35904580571), F IMPL-ACCEPTED + SOAK-OPEN (parallel track), G V1-CLOSED/ACCEPTED (+F SOAK-OPEN parallel), SLICE D DONE (authorized packet v2; kernel/kill/switch.{hpp,cpp} + test_kill 262 checks + test_noalloc_kill 0 allocs, normal+hardened; FSM re-attempt correction (prior_attempt_failed, one issuance per terminal failure, tie-breaks pinned); eval/actuation split, MEDIUM FSM exact, HARD protection-before-revoke asserted incl. unconfirmed-never-revokes + 112-combo ordering invariant; build.sh gates added; full battery + freeze green; Addendum 106), PARALLEL TRACKS (human ruling, plan/07 exception + doc 13 s13.5): Track A poller/graph/f2/ingest (BLS/BEA NEXT) || Track B D/H1 (D authorization consumed, Slice D DONE; H1 Authorization Packet v3 ACCEPTED as final text — sizing risk/veto-owned, kernel_state.hpp read-only ack-surface only, protected-entry-only normal path, one-attempt-one-query reconcile-first, §13.5 sequence respected with NO override: H1 spec/design may be prepared, H1 implementation AUTHORIZED (packet v3) and BUILT: kernel/exec/router.{hpp,cpp} (lifecycle-only FSM, journal-first, E1 protected-only, one-query reconcile, UNKNOWN-freeze, emergency exception) + kernel/log/journal.{hpp,cpp} (hash-chained rows, redaction gate) + kernel/broker/ (adapter interface, Alpaca-paper bracket adapter over injected transport, frozen ID recipe, paper fill model); suites router 83 + journal 23 + broker 41 + drills 74 + noalloc-exec 0, normal+hardened, build.sh gates added; full battery + freeze green (Addendums 108-110); no execution yet. SECOND CORRECTIVE (audit of f5dd11c): repair journals as frozen "reconcile" (no tenth kind; repair-journal-valid regression); broker UUID grammar for persisted IDs (zero-init crash fields, writer+reader strict, real-UUID regressions); DELETE 204-empty accepted (422/500 refuse); transport_ok distinguishes authoritative-absent from transport-failure (query-retry same identity); cancel_failed vs silence (silence re-checks, never UNKNOWN); corrupt-PROTECTED guard; 24-permutation distinct-event ordering converges (Addendum 110). THIRD CORRECTIVE (audit of 73a4509): repair journals frozen "reconcile" (FormatRow-valid regression); UUID-only persisted broker IDs (writer+reader strict, zero-init, real-UUID tests); DELETE 204-empty confirms; transport_ok (404-absent cancels terminally without UUID, malformed/5xx reconcile); ack authority split (422 terminal, ambiguous reconciles same-ID); strict legs-array protection proof (>=2 legs+ids+markers, unbalanced refused); bounded query budget kQueryMaxAttempts=3 persisted (exhaustion freezes, never loops/resends); cancel_failed vs silence; identity-tagged obs (foreign ignored) + 24-permutation distinct-event convergence; writer-canonical snapshots (Addendum 111). FOURTH CORRECTIVE (audit of 57cce89): retry-once budget kQueryMaxAttempts=2 (1 initial + 1 retry, no third; restart preserves attempts); single-query authoritative shapes documented (POST bracket legs for submit, by-client-ID Order entity for reconcile, no nested param, no second query); POST UUID captured on ack + persisted pre-cancel + survives snapshot + DELETE uses exactly it (E2E drill); legs proof binds roles by venue leg type (TP=limit, SL=stop-family, distinct ids, order-level TP/SL params, 1/3-leg + dup-id + dup-role + untyped refused); identity gate strict (matching applies, foreign + untagged ignored; 24 perms run tagged; duplicates idempotent; receipt-order tie-break frozen for the REST snapshot class); 4xx split (400/422 permanent, 401/403 auth, 429 rate, other ambiguous — never terminal CANCELLED); journal/persistence seam honesty (G0 runner owns append/fsync/load/retention/backup/summary) (Addendum 112)) CORRECTIVE PASS (independent audit of e86b57d): P0 protection-missing now repairs-or-flattens (REPAIR_SENT, JOURNAL_REPAIR, corrupt-PROTECTED guard, 13x2x64 protection invariant sweep); Alpaca transport corrected (GET lookup by client ID, DELETE cancel by UUID, no hidden state); OCO repair is LIMIT with side/price guards; durable snapshot/restore + crash-resume + randomized-convergence + outage-row + D-integration drills; cancelled-empty direct path; bad-state/enum fail-closed (Addendum 109)) || Track C F 24h soak (overlaps, never retro-blocks). F+ OPEN) → 13. Phase 4 paper loop G0 → 14-16. G1/G2/G3 gates. (10. Phase 2 JEV sidecar DONE, accepted/frozen `50ea369` + authorized hardening.)

## A. Agent orientation files (Phase-0, new)
- [x] A.1 DONE 2026-09-18: ARCHITECTURE.md (8 questions, MiroHedge-mapped) at root
- [x] A.2 DONE 2026-09-18: AGENTS.md session rules (read ARCH+00, TODO discipline, no secrets, fail closed) at root
- [x] A.3 DONE 2026-09-18: pi harness loads AGENTS.md every turn (human-confirmed)
- [x] POLLER TRACK / EDGAR SLICE (Round-7 code ACCEPTED at `27657b9`, hosted `35959871294` all-green; live operational evidence OPEN): production `research-plane/sources/edgar.py` (submissions + companyfacts + conditional ETag/Last-Modified CIK cache; MIRO_CONTACT UA fail-closed; 10/s token bucket monotonic; 3 retries + jitter; 429 one-halving-per-episode + 1h throttle + deterministic recovery; HTTPError normalized + explicitly closed; regulator acceptanceDateTime -> observed_at_ns when strictly valid incl. cross-date, else filingDate-midnight flagged observed_at_estimated, future acceptance dropped; parallel-array validation; full recent-array scan, 64-cap overflow counted as truncated; accession marked seen ONLY on emit; pinned entity_map sole CIK authority incl. companyfacts + symbol-resolved fetch; completion-stamped strict-schema heartbeat with tmp cleanup; harvest(watchlist, epoch) envelope; entity_ref {cik} on every record) + `research-plane/tests/test_edgar.py` (47 tests, fake transport/clock, warnings-as-errors, hosted-verified). No f2 invention, no resolver changes, no persistence authority, no egress hardcoding. Full plane suite: only pre-existing Windows env failures (langgraph-absent import, file-lock tear test); freeze PASS. OPEN gates (not code defects): live SEC soak + zero-403 + p50/p99 per 09 §9.4; shared 10/s deployment limiter (process-local now); estimated -> published_ns=None resolver wiring at H1 integration.
- [x] POLLER TRACK / FRED SLICE (code ACCEPTED, hosted 24/24 PROVEN run 36013253054 on 72ca59f):  (observations + ALFRED realtime, key fail-closed, 400 semantics, episode throttle, vintage realtime required, max-date latest) +  (24 tests, hosted-verified). Live soak/p50-p99/ALFRED-replay OPEN. Treasury/BLS/BEA NEXT.
- [x] POLLER TRACK / TREASURY SLICE (implementation/code ACCEPTED after independent re-audit; hosted suite green; live operational evidence OPEN: soak + p50/p99). BLS slice code ACCEPTED (independent re-audit of 7817f20, hosted 36036667089, 21 tests); live soak + p50/p99 OPEN parallel; BEA slice code ACCEPTED (independent audit of 9ef8956: 23/23 tests, credential + numeric corrections, downstream namespace integration, hosted 36042332716 green); live soak + p50/p99 OPEN parallel.:  (auctions_query page-64, cusip+record_date PK, strict dates/CUSIP, empty/all-dropped unhealthy, exact provenance) +  (18 tests, hosted step present from prior wiring). Live evidence OPEN. BLS/BEA NEXT.

- [x] TRACK A / PRODUCTION SEAM (INDEPENDENT ACCEPTANCE COMPLETE, 6fdeda7): runner composition + seam harvest/parser/watermarks/publisher + durable canonical projection + verified-only history recovery + checkpoint-resume emit, 25 seam + 7 graph tests, ARCHITECTURE reconciled, hosted WAE on evidence+plane, hosted 36056991773 all-green. Code acceptance only — live soak/p50-p99 per source stays OPEN as parallel operational evidence (not a retro-blocker). Soak round-1 (6 cycles/source, sandbox/source_soak.py, soak-evidence-live.json, linear-interpolation percentiles): EDGAR n=6 p50=94ms/p99=837.5ms ok; FRED n=6 p50=6000ms/p99=6265.2ms ok (6 series @ 1/s pace); Treasury n=6 p50=2711ms/p99=2887.7ms ok; BEA n=6 p50=992.5ms/p99=1029.4ms ok; BLS n=6 6/6 live-403 from this egress (adapter fail-closed PROVEN live, usable p50/p99 needs allowed egress). Nearest-rank round-1 p50s SUPERSEDED (Addendum 101). Extended soak n=30 (soak-evidence-extended.json): EDGAR p50=125/p99=1534.3 ok; FRED p50=6008/p99=7475.9 ok; Treasury p50=2563/p99=7076.9 ok; BEA p50=1016/p99=1147 ok; BLS 30/30 refused fail-closed. Live seam pass (seam-live-pass.json): 85 live records -> 85 fused -> 64 emitted (cap) -> ctx 63 accepted + 1 ttl-expired, BLS absent blocked nothing (Addendum 102). Stale/recovery runtime proof (stale-recovery.json, transport-layer fault injection on Treasury): A 85rec/63acc -> B fault x2 treasury-stale/absent, 0 treasury features, 31acc others, uncovered history dropped -> C recovered 63acc; heartbeat/TTL agree; live-pass harness now asserts accounting invariants itself (Addendum 103). Genuine longer soak n=120/source (soak-evidence-n120.json, raw 120 polls kept): EDGAR p50=125/p99=297, FRED 6016/7401.3, Treasury 2578/3226.3, BEA 1032/1725.3, BLS refusal-only; stale_recovery.py tightened to assert stamps+heartbeat-files+bundle/history contents+C==A composition (Addendum 104).