# Frozen Phase-2.5 research-plane dependencies (doc 08; exact pins).
# Reproducibility: every runtime dep is == pinned (no floating ranges).
# Install: python3 -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
langgraph==1.1.6
langgraph-checkpoint-sqlite==3.1.1
smolagents==1.26.0
pydantic==2.13.5
requests==2.34.2
beautifulsoup4==4.15.0
feedparser==6.0.14
lxml==6.1.3
pandas==3.0.6
tzdata==2026.4
